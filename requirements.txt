# RouteCare backend (FastAPI) — Python dependencies
# Install with:  pip install -r requirements.txt

fastapi==0.115.6           # Web framework: routers, validation, WebSocket support
uvicorn[standard]==0.34.0  # ASGI server that runs FastAPI (includes websockets + reload)
sqlalchemy==2.0.36         # ORM / database access layer
psycopg2-binary==2.9.10    # PostgreSQL driver used by SQLAlchemy
python-dotenv==1.0.1       # Loads configuration from the .env file
PyJWT==2.10.1              # Create and verify JWT authentication tokens
bcrypt==4.2.1              # Hash OTP codes (never stored in plaintext)
slowapi==0.1.9             # Rate limiting (OTP / SMS abuse protection)
